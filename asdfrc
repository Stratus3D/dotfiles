# Tell asdf to pull versions from other version files like .ruby-version
legacy_version_file = yes

# When upgrading upgrade to the latest release candidate version
use_release_candidates = yes

# Keep all downloads for convenience when working offline
always_keep_download = yes

# I don't want to be explicit about the plugin code I install
disable_plugin_short_name_repository = yes
