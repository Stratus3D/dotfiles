
" This isn't great because we're setting a global variable with a setting
" intended only for Java source code. Gradle test output is often verbose so
" we want a larger quickfix window to view failures in.
let g:dispatch_quickfix_height = 26
