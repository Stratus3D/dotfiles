% binary, encoding, data

# print the encoding of a character by the console. Useful for bash scripts. Also use `od` to dump octals.
echo -n ✘ | hexdump

#
echo -n ✘ | od

# Convert a string of binary 1s and 0s to ascii
for a in $($1); do printf "%x" $((2#$a)); done | xxd -r -p
